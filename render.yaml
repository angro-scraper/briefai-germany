services:
  - type: web
    name: briefai-germany-download
    runtime: static
    buildCommand: mkdir -p install-site/download && tar -xJf distribution/BriefAI-Germany-debug.tar.xz -C install-site/download
    staticPublishPath: ./install-site
    autoDeployTrigger: commit
    headers:
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
      - path: /*
        name: Referrer-Policy
        value: strict-origin-when-cross-origin
